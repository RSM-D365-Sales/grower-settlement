# Local development defaults — mock auth, local API. Safe to commit (no secrets).
VITE_AUTH_MODE=mock
VITE_API_BASE_URL=http://localhost:7071/api
VITE_ENTRA_CLIENT_ID=
VITE_ENTRA_TENANT_ID=
VITE_API_SCOPE=
