{
  "name": "grower-settlement-platform",
  "private": true,
  "version": "0.1.0",
  "description": "Convenience scripts only — packages are independent, no workspaces. Run npm install inside web/, api/, edge-agent/.",
  "scripts": {
    "dev:web": "npm --prefix web run dev",
    "dev:api": "npm --prefix api run dev",
    "build": "npm --prefix api run build && npm --prefix web run build",
    "test": "npm --prefix api test && npm --prefix web test",
    "typecheck": "npm --prefix api run typecheck && npm --prefix web run typecheck"
  }
}
