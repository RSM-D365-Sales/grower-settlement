{
  "name": "grower-edge-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Receiving-station edge agent: scale reads + ZPL label printing (Phase 4). Phase 0 stub.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "typescript": "^5.8.3"
  }
}
